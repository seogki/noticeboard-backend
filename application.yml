spring:
  datasource:
    url: jdbc:mariadb://13.124.126.111:3306/noticeboard
    driver-class-name: org.mariadb.jdbc.Driver
    username: seogki
    password: seogki
  # sql:
  #   init:
  #     mode: always
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        "[format_sql]": true
        "[show_sql]": true
logging:
  level:
    org:
      hibernate:
        SQL: DEBUG
        type:
          descriptor:
            sql: trace
jwt:
  secret: VlwEyVBsYt9V7zq57TejMnVUyzblYcfPQye08f7MGVA9XkHa

server:
  port: 8082
